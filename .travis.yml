language: python
python:
    - "2.6"
    - "2.7"
    - "3.3"
    - "3.4"
    - "3.5"
    - "pypy"
env:
  matrix:
    - TZ=UTC
    - TZ=Europe/Paris
    - TZ=Asia/Hong_Kong

install:
    - pip install -r requirements/ci.txt

script: nosetests --with-cov -v -w tests
after_success:
    - coveralls
